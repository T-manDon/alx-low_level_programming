#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - refers to the entry point
 *
 * Return: 0 (success)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
		putchar('\n');
	}

		return (0);
}
