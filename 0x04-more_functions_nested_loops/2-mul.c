#include "main.h"

/**
 * mul - multiplies two integers
 * @a: this is the first inter
 * @b: this is the common integer
 * Return: adds the multiplied values
 */

int mul(int a, int b)
{
	return (a * b);
}
