#include <stdio.h>
/**
 * main - The entry point of the program
 * Return: 0 (implies success of the code and returns to the main)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
