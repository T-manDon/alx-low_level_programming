#include <stdio.h>
/**
 * main - a program that uses the printf function
 * Return: 0 (the zero output indicates success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
